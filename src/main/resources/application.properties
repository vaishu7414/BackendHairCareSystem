spring.application.name=hair-care-ecommerce

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/haircare_db_new?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=${DB_DRIVER:com.mysql.cj.jdbc.Driver}

spring.jpa.database-platform=${DB_DIALECT:org.hibernate.dialect.MySQL8Dialect}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

jwt.secret=${JWT_SECRET:your_super_secret_key_that_should_be_at_least_256_bits_long_for_security_purposes_12345}
jwt.expiration=${JWT_EXPIRATION:86400000}

app.admin.name=${APP_ADMIN_NAME:System Admin}
app.admin.email=${APP_ADMIN_EMAIL:admin@haircare.com}
app.admin.password=${APP_ADMIN_PASSWORD:Admin@123}

server.port=8080
server.servlet.context-path=/api

# Email Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:pathakalka1248@gmail.com}
spring.mail.password=${MAIL_PASSWORD:zgnf cujh yunr hoat}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# OTP Configuration
otp.expiry.minutes=${OTP_EXPIRY:5}

logging.level.root=INFO
logging.level.com.haircare=DEBUG
#customer :-vaishu123 # pathakvaishnavi1248@gmail.com
